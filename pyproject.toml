[project]
name = "taskpilot"
version = "0.14.0"
description = "A local service exposing an HTTP API to spawn and manage long-running autonomous Claude Code agents"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
